# 串口调试命令说明

## 命令格式

```
device motor push=a,b,c,d
```

## 参数说明

| 参数 | 含义           | 范围        | 说明                                               |
| ---- | -------------- | ----------- | -------------------------------------------------- |
| a    | 电机转动时间   | 1 ~ 9999 ms | 电机实际运行持续时间（最小值为 1ms）               |
| b    | 电机延时时间   | 0 ~ 9999 ms | 接收到启动信号后，延时多久开始转动                 |
| c    | 电机速度百分比 | 5 ~ 100     | PWM 占空比，控制电机转速；大于 95 时强制限制为 95  |
| d    | 电机加速步距   | 0 ~ 50      | 每 10ms 占空比的增量，0 表示无加速直接到达目标速度 |

> **注意**：第四个参数 `d`（加速步距）为可选参数，省略时默认值为 0。

---

## 状态机实现说明

电机控制采用四状态状态机，在 `pusher_motor_loop()` 中以主循环方式运行：

```
┌─────────────┐    启动信号     ┌─────────────┐
│ MOTOR_STATE │ ──────────────> │ MOTOR_STATE │
│    IDLE     │                 │    WAIT     │
│   (空闲)     │                 │   (等待)     │
└─────────────┘                 └─────────────┘
      ▲                               │
      │                               │ 延时时间到
      │                               ▼
      │                         ┌─────────────┐
      │                         │ MOTOR_STATE │
      │                         │   RUNNING   │
      │                         │   (运行)     │
      │                         └─────────────┘
      │                               │
      │                               │ 运行时间到
      │                               ▼
      │                         ┌─────────────┐
      └─────────────────────────│ MOTOR_STATE │
                                │    STOP     │
                                │   (停止)     │
                                └─────────────┘
```

### 1. 电机转动时间（参数 a）的实现

- **设置位置**：`pusher_motor_set_params_and_start()` → `params_manager_set_direction_time()`
- **生效位置**：`MOTOR_STATE_RUNNING` 状态
- **实现逻辑**：
  - 进入 `RUNNING` 状态时，记录当前系统 tick：`motor_start_time = HAL_GetTick()`
  - 每次循环检查：`(current_time - motor_start_time) >= direction_time_ms`
  - 若条件满足，状态机从 `RUNNING` 跳转到 `STOP`

### 2. 电机延时时间（参数 b）的实现

- **设置位置**：`pusher_motor_set_params_and_start()` → `params_manager_set_wait_time()`
- **生效位置**：`MOTOR_STATE_WAIT` 状态
- **实现逻辑**：
  - 接收到启动信号后，从 `IDLE` 进入 `WAIT` 状态，记录 `motor_start_time = HAL_GetTick()`
  - 每次循环检查：`(current_time - motor_start_time) >= wait_time_ms`
  - 若条件满足：
    - 设置 `pusher_motor_work_flag = 1`
    - 重新记录 `motor_start_time = HAL_GetTick()`（作为运行时间的起点）
    - 进入 `RUNNING` 状态

### 3. 加速度（参数 d）的实现

- **设置位置**：CLI 解析时通过 `pusher_motor_set_acceleration(uint8_t accel)` 设置，范围 0~50
- **生效位置**：`MOTOR_STATE_RUNNING` 状态
- **相关变量**：
  - `accel_current_duty`：当前实际输出的占空比
  - `last_accel_tick`：上次调整占空比的时间戳
  - `acc_speed_value`：加速步距（每步增量）
  - `ACCEL_INTERVAL_MS`：固定为 10ms，每 10ms 调整一次占空比
- **实现逻辑**：
  - **进入 `RUNNING` 时**：
    - 若 `acc_speed_value == 0`（无加速）：直接输出目标占空比 `E1_Set_Duty(target_duty)`、`E2_Set_Duty(target_duty)`
    - 若 `acc_speed_value != 0`（有加速）：当前占空比从 0 开始，`accel_current_duty = 0`
  - **在 `RUNNING` 状态中**：
    - 每隔 `ACCEL_INTERVAL_MS`（10ms）检查一次
    - 若 `accel_current_duty < target_duty`：
      - 若差值大于 `acc_speed_value`：`accel_current_duty += acc_speed_value`
      - 否则：`accel_current_duty = target_duty`（到达目标）
    - 更新 PWM 输出：`E1_Set_Duty(accel_current_duty)`、`E2_Set_Duty(accel_current_duty)`
  - **进入 `STOP` 时**：
    - 占空比清零，`E1_Set_Duty(0)`、`E2_Set_Duty(0)`
    - `accel_current_duty` 重置为 100（为下次加速启动做准备）

### 4. 状态机状态说明

| 状态                  | 名称 | 说明                                     |
| --------------------- | ---- | ---------------------------------------- |
| `MOTOR_STATE_IDLE`    | 空闲 | 等待启动信号，电机不转                   |
| `MOTOR_STATE_WAIT`    | 等待 | 接收到启动信号，进行延时等待             |
| `MOTOR_STATE_RUNNING` | 运行 | 电机转动，同时进行运行时间判断和加速处理 |
| `MOTOR_STATE_STOP`    | 停止 | 停止电机，清除标志，返回 IDLE            |

---

## 使用示例

### 示例 1：无加速（直接到达目标速度）

```
device motor push=1000,500,80
```

表示：

- 电机转动 1000ms
- 启动后延时 500ms 开始转动
- 目标速度为 80% PWM 占空比
- 无加速（直接到达目标速度，第四个参数省略默认为 0）

### 示例 2：带加速

```
device motor push=1000,500,80,5
```

表示：

- 电机转动 1000ms
- 启动后延时 500ms 开始转动
- 目标速度为 80% PWM 占空比
- 每 10ms 增加 5% 占空比，从 0 逐步加速到 80%
